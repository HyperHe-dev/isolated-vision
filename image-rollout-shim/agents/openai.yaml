interface:
  display_name: "Image Rollout Shim"
  short_description: "Route local view_image calls through an isolated exec"
  default_prompt: "Use $image-rollout-shim instead of view_image for every local image inspection."
policy:
  allow_implicit_invocation: true
